import processing.core.*;
public class gameDriver {

	PApplet p;
	public gameDriver(PApplet parent){
		p = parent;
	}
	public void draw()
	{
		
		p.background(255);
		p.rect(0, 0, 10000, 1000);
	}
}
//merge test
